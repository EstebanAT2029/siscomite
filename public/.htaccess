Options -Indexes

<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /siscomite/public/

# Siempre redirigir todo a index.php manteniendo ?url=
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
</IfModule>

